| Utility | Quality Attribute    | Attribute Refinement    | ASR                                                                                                                                                                                                                     |
| ------- | -------------------- | ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Utility | Performance          | Response time           | When a customer opens the restaurant list under typical mobile network conditions, the first page renders in ≤ 2 seconds (H,M).                                                                                         |
|         |                      |                         | When a customer performs restaurant or item search under normal conditions, the backend returns the first page within p95 ≤ 2 seconds with pagination supported (H,M).                                                  |
|         |                      |                         | When an admin loads the monitoring view with 1,000 active orders, the page load meets p95 ≤ 2 seconds and displayed data freshness is ≤ 60 seconds (M,M).                                                               |
|         |                      | Update propagation      | When an order status changes, the customer app receives the update within ≤ 5 seconds under normal conditions (H,H).                                                                                                    |
|         |                      |                         | When a restaurant updates availability under peak load, the change is applied and visible to customers within ≤ 10 seconds (H,M).                                                                                       |
|         |                      |                         | When a restaurant edits menu price/name/availability, changes propagate to customers within ≤ 60 seconds (M,M).                                                                                                         |
|         | Scalability          | Peak load growth        | When browse/search load reaches 2× projected peak, the system still meets p95 search response ≤ 2 seconds in load tests (H,H).                                                                                          |
|         | Usability            | Learnability            | When a first-time customer registers and logs in with valid data, the flow completes within ≤ 2 minutes for ≥ 90% of test participants and yields SUS ≥ 80 (H,M).                                                       |
|         |                      | Task success            | When customers attempt a “find a restaurant and open its menu” task, ≥ 90% complete the task successfully in a usability test with predictable navigation (H,M).                                                        |
|         |                      | Proficiency             | When a shipper toggles availability, the action completes in ≤ 2 interactions and the resulting state plus sync outcome is clearly visible (M,L).                                                                       |
|         |                      | Normal operations       | When a customer submits a restaurant/shipper rating for a delivered order, ≥ 90% complete the submission within ≤ 30 seconds and any validation errors are actionable (M,L).                                            |
|         | Availability         | Uptime                  | When customers or partners attempt to authenticate during a calendar month, the authentication endpoints achieve uptime ≥ 99.9% and failures degrade gracefully with retryable errors (H,M).                            |
|         |                      |                         | When delivering order status updates during a calendar month, the real-time channel achieves uptime ≥ 99.9% and falls back to polling without app breakage when degraded (H,H).                                         |
|         | Reliability          | State machine integrity | When any actor attempts an out-of-sequence order status transition, the platform rejects 100% of invalid transitions with a specific error and does not corrupt order state (H,H).                                      |
|         |                      | Exactly-once semantics  | When an assigned shipper confirms delivery with retries/offline recovery, the system records the delivery confirmation exactly once and the updated status is visible within ≤ 5 seconds under normal conditions (H,H). |
|         |                      | Idempotency             | When the client retries checkout using the same idempotency key within `ORDER_IDEMPOTENCY_TTL_MINUTES`, the backend returns the same order ID and creates zero duplicate orders (H,H).                                  |
|         |                      | Atomicity               | When a shipper accepts a dispatch request, the order is assigned atomically such that ≤ 1 shipper can be assigned to the same order (H,H).                                                                              |
|         | Security             | Authorization           | When an unauthenticated or unauthorized user attempts any admin endpoint, 100% of requests are denied with 401/403, errors do not leak sensitive details, and the attempt is recorded for investigation (H,H).          |
|         |                      | Integrity               | When VNPay callbacks are received, the system verifies callback integrity and ensures orders are only created/routed after successful confirmation, preventing forged confirmations in security tests (H,H).            |
|         |                      | Input safety            | When reviews are stored and later displayed, inputs are validated/sanitized such that stored-XSS does not execute in security regression tests and only the authenticated order owner can submit feedback (M,M).        |
|         | Interoperability     | External services       | When the platform calls geocoding/maps services for deliverability/proximity checks, adapter calls succeed at ≥ 99% with configured timeouts/retries and provide a clear fallback when the provider is degraded (H,M).  |
|         |                      | Multi-channel delivery  | When order status changes occur across foreground/background app states, notification adapters (push/WebSocket/polling) succeed at ≥ 99% with timeout/retry behavior and deterministic fallback (H,H).                  |
|         | Manageability        | Operational control     | When an admin approves or rejects a restaurant/shipper application, the decision takes effect within ≤ 1 minute and the pending queue supports paging/filtering (M,M).                                                  |
|         |                      | Configuration changes   | When administrators change operational policies (service area boundary, stuck thresholds, RBAC policy), the changes take effect within ≤ 5 minutes without code changes (M,H).                                          |
|         | Supportability       | Diagnostics             | When an order remains in a status beyond a configured threshold, the system flags it with a reason code including the breached threshold and exposes drill-down status history for triage (M,M).                        |
|         |                      | Traceability            | When a privileged action is denied or fails, the system records actor, target, outcome, timestamp, and correlation ID such that 100% of incidents can be queried by time/actor/action type (M,M).                       |
|         | Testability          | Automated verification  | When lifecycle rules change, automated transition-matrix tests cover 100% allowed/forbidden edges and assert expected outcomes deterministically (M,M).                                                                 |
|         | Maintainability      | Routine changes         | When a new order status is added, implementation requires changes in ≤ 2 modules and passes contract/transition tests (M,M).                                                                                            |
|         | Flexibility          | Product evolution       | When a new payment provider is introduced, it is added behind a provider abstraction without breaking the checkout contract and passes contract tests for success/failure/cancel flows (M,H).                           |
|         | Conceptual Integrity | Consistent model        | When any module reads or writes order status, it uses a single shared status model and contract tests assert the allowed status set so no module introduces conflicting semantics (H,H).                                |
|         | Reusability          | Shared subsystems       | When new real-time events are added, they reuse the same event envelope, routing, retries, and channel adapters rather than duplicating delivery logic (M,H).                                                           |
